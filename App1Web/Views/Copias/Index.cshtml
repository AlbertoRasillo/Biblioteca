@model IEnumerable<App1Web.Models.Copias>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.comentarios)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Obra.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.comentarios)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Obra.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.n_copia }) |
            @Html.ActionLink("Details", "Details", new { id=item.n_copia }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.n_copia })
        </td>
    </tr>
}

</table>
